## 目标
- 保持并排布局不变，将每个 `<canvas>` 在其卡片容器中居中显示，提升美观度。

## 实现
- 为 `<canvas>` 添加居中样式：`className="mx-auto block"`，实现水平居中。
- 保留当前缩放与尺寸设置逻辑，不影响一屏展示与坐标缩放的准确性。

## 变更文件
- `frontend/src/app/page.tsx`：为两个 `<canvas>` 标签添加居中样式类名。

## 验证
- 上传 `1.json` 后，两侧画布居中显示；比较后仍保持等比例缩放与正确描边。