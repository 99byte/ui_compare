## 目标
- 点击“开始比较分析”后，画布继续显示上传时解析出来的原始 UI 元素高亮，不被替换为匹配集合，不做叠加对比绘制。

## 修改点
- 文件：`frontend/src/app/page.tsx`
- 在比较成功回调中仅更新 `reportId/blueprints/screen`，不改变画布元素集合：
  - 位置：`frontend/src/app/page.tsx:267-284`
  - 修改：将两处 `drawComponentsOnCanvas(...)` 调用的 `components` 参数改为原始集合：
    - 设计侧：`designComponents`
    - 代码侧：`codeComponents`
  - 同时将 `unmatchedComponents` 参数改为 `undefined`，避免叠加对比结果导致视觉变化。
  - 示例：
    - 设计侧：`drawComponentsOnCanvas(designCanvasRef.current, designComponents, '#ff003c', undefined, data.screen || getScreenSize(designJson))`
    - 代码侧：`drawComponentsOnCanvas(codeCanvasRef.current, codeComponents, '#00f3ff', undefined, data.screen || getScreenSize(codeJson))`

## 不改动内容
- 初次与后续 `useEffect` 的原始元素绘制逻辑保持不变：`frontend/src/app/page.tsx:225-237`
- 后端接口与 Planner 输出保持不变。

## 验证
- 上传两份 JSON，点击“开始比较分析”。
- 期望：左/右画布仍显示原始元素高亮，视觉不消失；右侧“Planner 报告”仍正常更新（但不影响画布内容）。